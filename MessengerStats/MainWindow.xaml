<Window x:Class="MessengerStats.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MessengerStats"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="{StaticResource Background}">
        <ScrollViewer Style="{StaticResource StylizedScrollBar}"  VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <StackPanel Height="500">
                <Grid Height="100" Background="{StaticResource BackgroundDark}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
