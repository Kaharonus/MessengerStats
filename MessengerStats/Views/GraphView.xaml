<UserControl x:Class="MessengerStats.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessengerStats.Views"
             xmlns:ScottPlot="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Loaded="Grid_Loaded" Background="{StaticResource BackgroundDark}">
        <Grid Height="40" VerticalAlignment="Top" Background="{StaticResource BackgroundDark}">
            <ComboBox x:Name="GraphsSelect" Style="{DynamicResource StyledComboBox}"  Width="150" HorizontalAlignment="Right" Margin="0,0,15,0" Height="30" >

            </ComboBox>
        </Grid>
        <Grid x:Name="PlotPanel" Margin="0,40,0,10" />

    </Grid>
</UserControl>
